% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myclt_mean.R
\name{myclt_mean}
\alias{myclt_mean}
\title{Central Limit Theorem Simulation for Sample Means (Uniform Distribution)}
\usage{
myclt_mean(n = 10, iter = 10000, a = 0, b = 5)
}
\arguments{
\item{n}{Integer. Sample size for each iteration. Default is 10.}

\item{iter}{Integer. Number of iterations (simulated samples). Default is 10000.}

\item{a}{Numeric. Lower bound of the uniform distribution. Default is 0.}

\item{b}{Numeric. Upper bound of the uniform distribution. Default is 5.}
}
\value{
A numeric vector of length \code{iter} containing the simulated sample means.
A histogram is produced as a side effect.
}
\description{
Simulates the sampling distribution of the sample mean by drawing repeated
samples from a Uniform(a, b) distribution. Produces a histogram of the
simulated sample means overlaid with a theoretical normal curve based on
the Central Limit Theorem.
}
\details{
For a Uniform(a, b) distribution, the population mean is \eqn{\mu = (a+b)/2}
and the population variance is \eqn{\sigma^2 = (b-a)^2/12}. By the Central
Limit Theorem, the distribution of the sample mean \eqn{\bar{Y}} approaches
\eqn{N(\mu, \sigma^2/n)} as \eqn{n} increases.
}
\examples{
# Basic usage with defaults
w <- myclt_mean()

# Custom parameters
w <- myclt_mean(n = 30, iter = 10000, a = 0, b = 10)
mean(w)
var(w)

}
